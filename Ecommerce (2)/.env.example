# Docker Compose variable substitution (copy to .env on the droplet)
# Discover network: docker inspect postgres --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}'
POSTGRES_NETWORK=trackit_default
